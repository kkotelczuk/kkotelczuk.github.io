language: node_js                               

node_js:                                      
  - 6.9.4   

build:                                                  

  ci:
    - npm install -g hexo
    - npm install
    - hexo generate
    - hexo deploy